# Telegram-Obsidian-Bridgeа Bot: Автоматизация Заметок
*Телеграм-бот для автоматического сбора сообщений, их обработки через Google Gemini и сохранения в виде Markdown-заметок в папке Obsidian.*

## 💬 Что это?

Этот Python-бот на базе `aiogram` служит мостом между вашим Telegram-аккаунтом и средой для ведения заметок Obsidian, используя возможности генеративного ИИ Google Gemini. Он разработан для IT-специалистов, студентов и пентестеров, которым нужно быстро структурировать информацию, полученную через Telegram, и сохранить её в удобном формате.

### Ключевые особенности:

*   **Буферизация сообщений:** Бот собирает несколько последовательных сообщений в Telegram от одного пользователя в течение короткого промежутка времени (`COLLECTION_DELAY`), объединяя их в один запрос для Gemini. Это позволяет отправлять большие куски текста, не прерывая мысли.
*   **Генерация Markdown:** Объединенный текст отправляется в Google Gemini с заданным `SYSTEM_INSTRUCTION` (описание для *этого* бота), который форматирует его в чистую Markdown-заметку, следуя вашим правилам (H1, H2, callouts, теги и т.д.).
*   **Интеграция с Obsidian:** Сгенерированная заметка автоматически сохраняется в указанную папку (`OBSIDIAN_INBOX_PATH`), готовую для синхронизации с Obsidian (например, через Syncthing на RPi или Termux).
*   **Гибкая настройка:** Все ключевые параметры (токены API, пути сохранения, задержки) легко конфигурируются.

> [!INFO]
> Этот бот идеально подходит для быстрой фиксации идей, команд, ссылок или любой другой информации "на ходу" с последующей автоматической структуризацией и добавлением в вашу базу знаний Obsidian.

## 🚀 Как скачать и установить?

### 1. Клонирование репозитория

```bash
git clone https://github.com/your-username/telegram-gemini-obsidian-bot.git
cd telegram-gemini-obsidian-bot
```

### 2. Установка зависимостей

Убедитесь, что у вас установлен `pip`.

```bash
pip install -r requirements.txt
```

> [!TIP]
> Файл `requirements.txt` должен содержать:
> ```
> aiogram
> google-generativeai
> ```

## ⚙️ Как настроить?

Для работы бота вам потребуется получить API ключи и настроить пути.

### 1. Получение API ключей

*   **Telegram Bot Token (`API_TOKEN`):**
    *   Создайте нового бота через [@BotFather](https://t.me/BotFather) в Telegram.
    *   Сохраните полученный токен.
*   **Google Gemini API Key (`GEMINI_KEY`):**
    *   Перейдите на [Google AI Studio](https://aistudio.google.com/app/apikey).
    *   Создайте новый API ключ.
    *   Убедитесь, что у вас есть доступ к модели `gemini-2.5-flash` или аналогичной.

### 2. Конфигурация в коде или через переменные окружения

Вы можете напрямую указать значения в файле `main.py` или использовать переменные окружения для лучшей безопасности. Рекомендуется использовать переменные окружения, особенно для хостинга.

#### Прямое указание в `main.py`:

```python
# --- КОНФИГУРАЦИЯ ---
API_TOKEN = "ВАШ_ТЕЛЕГРАМ_БОТ_ТОКЕН"
GEMINI_KEY = "ВАШ_GOOGLE_GEMINI_API_КЛЮЧ"
OBSIDIAN_INBOX_PATH = "/data/data/com.termux/files/home/storage/downloads/Obsidian/nosort" # Пример пути для Termux/RPi
COLLECTION_DELAY = 2.5 # Время ожидания следующего сообщения в секундах
```

#### Использование переменных окружения (рекомендуется):

Создайте файл `.env` в корневой директории проекта:

```
API_TOKEN=ВАШ_ТЕЛЕГРАМ_БОТ_ТОКЕН
GEMINI_KEY=ВАШ_GOOGLE_GEMINI_API_КЛЮЧ
OBSIDIAN_INBOX_PATH=/data/data/com.termux/files/home/storage/downloads/Obsidian/nosort
COLLECTION_DELAY=2.5
```

И измените загрузку переменных в `main.py` (потребуется `python-dotenv`):

```bash
pip install python-dotenv
```

```python
import os
from dotenv import load_dotenv

load_dotenv() # Загружаем переменные из .env

API_TOKEN = os.getenv("API_TOKEN")
GEMINI_KEY = os.getenv("GEMINI_KEY")
OBSIDIAN_INBOX_PATH = os.getenv("OBSIDIAN_INBOX_PATH", "/data/data/com.termux/files/home/storage/downloads/Obsidian/nosort")
COLLECTION_DELAY = float(os.getenv("COLLECTION_DELAY", 2.5))
```

> [!WARNING]
> Никогда не публикуйте ваши `API_TOKEN` и `GEMINI_KEY` в открытом доступе (например, на GitHub). Используйте переменные окружения или локальные конфигурационные файлы, исключенные из системы контроля версий.

### 3. Настройка `OBSIDIAN_INBOX_PATH`

Это критически важный параметр. Укажите полный путь до папки, куда Obsidian (или ваша система синхронизации, например, Syncthing) будет забирать новые заметки.

*   **Пример для Termux на Android (часто используется с RPi в связке):**
    `/data/data/com.termux/files/home/storage/downloads/Obsidian/nosort`
*   **Пример для Linux/RPi:**
    `/home/user/ObsidianVault/Inbox` или `/mnt/user/Obsidian/inbox`
*   **Пример для Windows (для тестирования):**
    `C:\\Users\\YourUser\\Documents\\ObsidianVault\\Inbox` (или `.\\` для папки проекта)

### 4. Запуск бота

После настройки всех параметров, вы можете запустить бота:

```bash
python main.py
```

Бот начнет опрашивать Telegram на наличие новых сообщений. Отправьте ему текст, подождите `COLLECTION_DELAY`, и он обработает ваши сообщения.

> [!TIP]
> Для постоянной работы на RPi или сервере, используйте `screen`, `tmux` или `systemd` сервис.
> Пример запуска через `screen`:
> ```bash
> screen -S telegram_bot
> python main.py
> # Ctrl+A D для отсоединения
> ```
